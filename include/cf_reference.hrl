%%====================================================================
%% Static syntax
%%====================================================================

-type e() :: {str, string()}
           | {file, string()}.

-type tp() :: 'Str'
            | 'File'.

-type arg() :: {atom(), string(), tp()}.